# 架构决策.md · 每日自我监督系统（成长飞轮 GrowthFlywheel）

> 审计日期：2026-08-13
> 审计人：架构师（依据 `D:\生财有术\AI工具箱\团队配置\01-架构师.md`）
> 当前版本：v3.0.0（全量落盘 124 文件，2026-08-12）
> 仓库：github.com/dawn-don-888/daily-self-supervision-system

---

## ① 代码完整性审计表

### A. 已实现（可视为完整）

| 模块 | 文件 | 状态 |
|------|------|------|
| 数据层 | `src/lib/storage.ts`（857 行） | ✅ 完整。实现全部 **14 个** localStorage key，与交接文档一一对应 |
| 常量/工具 | `constants.ts`(144行) / `utils.ts` / `chart-colors.ts` | ✅ 完整 |
| 依赖清单 | `package.json` | ✅ 完整（React 19 / Vite 8 / Tailwind v4 / shadcn / echarts / framer-motion / react-router 7） |
| UI 组件库 | `src/components/ui/`（**72 个** shadcn 组件） | ✅ 完整，远超交接文档所述 55 个 |
| 页面模块 | `src/pages/`（9 个模块 + 子组件：Today/Learning/Life/Stats/Profile/Welcome/Achievement/Example/NotFound） | ✅ 主体完整 |
| 业务数据 | `src/data/`（8 个 .ts 数据文件） | ✅ 完整 |
| 路由 | `src/app.tsx` | ✅ 6 条路由 + OnboardingGuard + 404 |
| 布局/导航 | `Layout.tsx` / `BottomTabBar.tsx`（滚动隐藏） | ✅ 完整 |
| 构建脚本 | `scripts/dev.mjs`（端口8001）/ `scripts/build.sh` / `setup-git-hooks.mjs` | ✅ 完整 |
| 文档 | 需求整理 / 开发SOP / 交接文档 / 对话记录 / 小红书MVP | ✅ 齐全 |

### B. 缺失 / 未完成

| 编号 | 缺失项 | 影响 | 严重度 |
|------|--------|------|--------|
| B-1 | **依赖未安装**（`node_modules` 不存在） | 当前无法 `npm install` 后本地 `typecheck/build` 验证 | 高（无法自证可运行） |
| B-2 | **`shared/` 目录不存在** | vite/tsconfig 的 `@shared` 别名、build.sh 的 `shared/static`、`shared/capabilities` 拷贝全部指向空目录 → 死配置 | 中（当前无静态资源故未爆，但属隐患） |
| B-3 | **`AchievementPage` 孤儿页** | 文件有默认导出，但无任何路由注册、无导航入口、无其它文件 import；成就实际由 `ProfilePage` 渲染 → 该文件为死代码 | 低（功能不漏，但冗余） |
| B-4 | **`架构决策.md` 本身缺失** | 01-架构师.md 明确要求的架构师交付物此前未产出（本次补） | 中（决策未固化） |
| B-5 | **平台决策未裁决** | `@lark-apaas` 取舍未做正式决策（见②） | 高（决定代码能否独立运行） |

### C. 矛盾 / 文档与代码不符

| 编号 | 矛盾点 | 文档说法 | 代码实况 |
|------|--------|----------|----------|
| C-1 | 静态数据位置 | README："静态数据文件 → `shared/static/data/`（JSON）" | 实际在 `src/data/*.ts`，且 `shared/` 根本不存在 |
| C-2 | UI 组件数量 | 交接文档："55 个 shadcn 组件" | 实际 `src/components/ui/` 有 **72 个** 文件 |
| C-3 | 根配置文件 | 交接文档根目录清单列 `tailwind.config.js / postcss.config.js` | **实际不存在**（Tailwind v4 用 CSS `@import`，无需这两文件） |
| C-4 | 入口组件名 | README："首页路由指向 `<Welcome />`" | 实际为 `<WelcomeGuide />`，`app.tsx` 已正确引用 |
| C-5 | 运行状态 | 交接文档待办："v3功能用户验收" 未完成 | 代码层面 v3 功能已落盘，但**未做用户验收**、**未做代码审计**（即本次任务） |

---

## ② 平台决策与理由（⚠️ 待用户拍板）

架构师首个任务 #2 要求裁决：**「独立 Web App（Vite+React，本地优先+可选云同步）」还是「保留飞书 APAAS」**。

> **⚠️ 决策已在 Phase 0 拍板（2026-08-13 09:27）**：用户明确选择「纯前端 Web App（Vite+React，浏览器跑，本地存储）」，数据后端"听架构师建议、倾向本地优先"。
> 因此这**不是待决问题，而是"既定方向 vs 当前代码"的漂移**——当前代码仍深度绑定飞书 `@lark-apaas`，与 Phase 0 决策矛盾，应**执行解耦**（见下「方案乙执行」），无需再次征求方向。

### 现状证据（@lark-apaas 耦合点 — 即偏离 Phase 0 决策的证据）
- `src/index.tsx` → `AppContainer` / `ErrorRender`
- `src/index.css` → `@import "@lark-apaas/client-toolkit-lite/styles.css"`
- `src/lib/storage.ts` → `scopedStorage`（**核心数据层，所有 localStorage 走它**）
- `src/pages/LifePage/sections/BrainTracker.tsx` → 动态 `import('@lark-apaas/client-toolkit-lite')`
- `src/pages/LifePage/sections/InspirationBoard.tsx` → `UniversalLink`（飞书跨应用跳转）
- `vite.config.ts` → `defineConfig` 来自 `@lark-apaas/coding-preset-vite-react`
- `tsconfig.app.json` → `extends "@lark-apaas/coding-presets-react/lib/tsconfig/..."`

### 方案对比

| 维度 | 方案甲：保留飞书 APAAS | 方案乙：独立 Web App（推荐基线） |
|------|----------------------|----------------------------------|
| 运行 | 必须走飞书构建链 + 飞书运行时 | `npm run dev` 即可本地跑，可部署任意静态托管 |
| 数据 | `scopedStorage`（飞书命名空间，随应用卸载清空） | 原生 `localStorage`（用户浏览器，持久可控） |
| 依赖 | 3 个 `@lark-apaas/*` 强依赖 | 去除后零平台耦合 |
| 风险 | 绑定飞书平台，账号/政策变动即失效 | 需自建导出/同步（可用现有导入导出） |
| 作品集价值 | 只能演示飞书内应用 | 可独立部署链接，转行 AI PM 作品集更硬 |
| 改动量 | 0（维持现状） | 约 5 处文件、数据层单点替换（storage.ts 集中，可控） |

**架构师建议**：**方向已定（Phase 0 = 独立 Web App），直接执行方案乙（独立化）**，理由——① 这是东东的转行作品集，独立可部署链接说服力远强于"只能在飞书里打开"；② 平台耦合集中在 `storage.ts` 单点 + 4 处轻量引用，迁移成本可控；③ 数据主权在用户浏览器更安全；④ 与 Phase 0 用户决策完全一致，无需再议方向，只需落地改造。

> 执行方案乙（解耦飞书）最小改造清单：
> 1. `storage.ts`：`scopedStorage` → 原生 `localStorage` 封装（API 同名，调用方零改动）
> 2. `index.tsx`：去掉 `AppContainer/ErrorRender`，用 `react-error-boundary` 直接包
> 3. `index.css`：去掉 `@lark-apaas` 样式 import
> 4. `InspirationBoard.tsx`：`UniversalLink` → `<a>`/`<Link>`
> 5. `vite.config.ts` / `tsconfig.app.json`：去掉飞书 preset，改标准 Vite/TS 配置
> 6. `package.json`：移除 3 个 `@lark-apaas/*` 依赖

---

### ②-补：决策卡（decision-master 固化 · 2026-08-13）

> 用「高维决策九步法」收敛。本决策属"中等偏重"，走五层决策系统 + 周易辩证/系统科学/女娲范式 三核心模块。

**一、问题界定**
- 背景：v3.0.0 代码通过 `@lark-apaas` SDK 跑在飞书 APAAS 运行时；Phase 0（2026-08-13）用户已拍板"纯前端 Web App + 本地存储"。
- 核心问题：代码现状与既定方向背离（平台耦合），须做"保留飞书 / 独立化 / 混合"取舍。
- 利益相关方：东东（作品集 owner）、后续接手 AI、潜在面试评审。

**二、阴阳两面**
- 阳（独立化有利）：可独立部署链接、作品集更有说服力；数据主权在用户浏览器；零平台依赖、不被飞书政策绑架；迁移成本可控（耦合集中在 storage.ts 单点 + 4 处轻引用）。
- 阴（独立化风险）：需自建导出/同步（现有导入导出可复用）；短期无飞书"一键发布"便利。
- 转化条件：只要 storage.ts 抽象层到位，风险可控 → 阳面占优。

**三、系统分析**
- 核心要素：数据层(scopedStorage)、入口(index.tsx AppContainer)、链接(InspirationBoard UniversalLink)、构建链(vite/tsconfig preset)。
- 要素关系：数据层是枢纽，其余 4 处均为"数据层 + 平台 API"的轻引用；改数据层即可解 80% 耦合。
- 反馈机制：解耦后 `npm run dev` 即可本地验证 → 正反馈加速后续迭代。

**四、方案对比**
| 方案 | 核心思路 | 收益 | 风险 | 适合条件 |
|------|---------|------|------|----------|
| 甲 保留飞书 | 维持 @lark-apaas | 零改动 | 绑定平台、不可独立演示、与 Phase0 矛盾 | 仅当确定永远只跑飞书 |
| **乙 独立化** ✅ | storage→原生localStorage + 去平台引用 | 可部署、主权在己、契合作品集 | 需 6 处改造 | **Phase0 既定方向** |
| 丙 混合 | 飞书运行时 + 兼容层 | 双轨 | 复杂度高、维护两倍 | 暂不需要 |

**五、推荐方案：乙（独立化）**
- 核心理由：与 Phase 0 用户决策一致；作品集价值最大；技术债务最低（耦合单点化）。
- 关键成功因素：storage.ts 抽象层 API 签名保持兼容，使 14 个调用方零改动。
- 应急预案：若某飞书特有行为（如 UniversalLink 跨应用跳转）无原生等价，降级为普通 `<a>` + 站内路由，功能不降级。

**六、行动路线（闭环）**
- 第一阶段（0-3 天）：storage.ts 抽象 localStorage 封装 → 替换 4 处轻引用 → 去 vite/tsconfig preset → `npm install` 后 `typecheck/build` 自证可跑。
- 第二阶段（1 周）：`npm run dev` 真机验证 5 页 + 积分/防刷逻辑；更新 交接文档/README 消除文档漂移。
- 第三阶段（持续）：可选 CloudBase/静态托管部署，拿到独立演示链接；Pro版与 agent 化另行立项。
- 反馈机制：每步跑 lint/typecheck，合并前过 QA 门禁。

---

## ③ 模块边界图（ASCII）

```
┌──────────────────────────────────────────────────────────┐
│                      入口 index.tsx                        │
│   (当前: AppContainer@lark | 方案乙: 纯 React Root)        │
└───────────────┬──────────────────────────────────────────┘
                │
┌───────────────▼──────────────────────────────────────────┐
│                  App (app.tsx)                            │
│   OnboardingGuard → 未引导则 WelcomeGuide                 │
│   Routes: /  /learning  /life  /stats  /profile  /welcome │
└───────────────┬──────────────────────────────────────────┘
                │
┌───────────────▼──────────────────────────────────────────┐
│   Layout (Outlet) + BottomTabBar (5 Tab 滚动隐藏)          │
└───────────────┬──────────────────────────────────────────┘
                │
   ┌────────────┼──────────────┬──────────────┬─────────────┐
   ▼            ▼              ▼              ▼             ▼
 TodayPage   LearningPage   LifePage     StatsPage    ProfilePage
 (打卡/心情/  (AI开发120/   (记账/健身/   (KPI/热力图  (等级/徽章/
  一问/复盘/   AIPM60/Vibe/  待办/灵感/    /心情趋势/   导出导入/
  灵感)       英语)          健脑)         周报)         提醒/Pro)
   │            │              │              │             │
   └────────────┴──────┬───────┴──────────────┴─────────────┘
                       ▼
        ┌──────────────────────────────────────┐
        │   数据层 lib/storage.ts（唯一出口）    │  ← 当前依赖 @lark-apaas scopedStorage
        │   14 个 localStorage key 全实现        │
        └───────────────┬──────────────────────┘
                        ▼
        ┌──────────────────────────────────────┐
        │   常量/工具 lib/constants utils chart-colors │
        │   业务数据 src/data/*.ts（8 文件）     │
        └──────────────────────────────────────┘

UI 层：components/ui（72 shadcn，禁止改） + components（Layout/BottomTabBar）
状态层：React hooks + storage 封装（无 Redux/Zustand，符合决策）
```

---

## ④ 技术栈建议

| 层 | 当前选型 | 建议 | 说明 |
|----|----------|------|------|
| 框架 | React 19 + Vite 8 + TS 5.9 | ✅ 保留 | 现代主流，合适 |
| 样式 | Tailwind v4 + shadcn(new-york) | ✅ 保留 | 72 组件已就绪 |
| 图标 | lucide-react | ✅ 保留（符合 P0 规则，非 emoji） | — |
| 图表 | ECharts + Recharts | ✅ 保留 | 双库略冗余，可后期精简 |
| 路由 | react-router-dom 7 | ✅ 保留 | — |
| 动画 | framer-motion | ✅ 保留 | — |
| 数据持久化 | **`@lark-apaas/client-toolkit-lite` scopedStorage** | ⚠️ **方案乙下弃用**，改原生 localStorage | 当前最大耦合点 |
| 构建 | `@lark-apaas/coding-preset-vite-react` | ⚠️ **方案乙下弃用**，改标准 `@vitejs/plugin-react` | — |
| 错误边界 | `@lark-apaas/client-toolkit-lite` ErrorRender | ⚠️ **方案乙下弃用**，用 react-error-boundary（已在依赖） | — |

**对 P0 规则（来自 MVP 专家团）的合规检查**：当前代码用 `lucide-react` SVG 图标（非 emoji）、无紫粉渐变、无明显 AI 模板味文案 → 基本合规，无需返工。

---

## ⑤ 风险与待办清单

### 风险
| 风险 | 等级 | 说明 |
|------|------|------|
| R-1 平台单点绑定 | 高 | `@lark-apaas` 未裁决前，工程无法本地独立运行/演示 |
| R-2 无法自证可构建 | 高 | node_modules 未装，未跑过 typecheck/lint/build |
| R-3 死配置 | 中 | `@shared` 别名 + build.sh 的 shared/ 拷贝指向不存在目录 |
| R-4 文档漂移 | 中 | README/交接文档与代码多处不符，易误导后续 AI/接手人 |
| R-5 孤儿代码 | 低 | AchievementPage 未被引用 |

### 待办（按优先级）
- [ ] **P0** 用户裁决平台方向（飞书 APAAS vs 独立 Web App）——见②
- [ ] **P0** `npm install` 后跑 `typecheck` + `lint` + `build`，验证可构建
- [ ] **P1** 根据②决策执行最小改造（若选独立化，6 处改动）
- [ ] **P1** 删除/补齐 `shared/` 死配置（要么建目录放静态资源，要么从 vite/tsconfig/build.sh 移除）
- [ ] **P1** 校正文档：README 的 shared 路径/组件数/旧结构；交接文档删掉不存在的 tailwind/postcss 文件
- [ ] **P2** 处理 AchievementPage：要么注册路由+加导航，要么删除死代码
- [ ] **P2** 完成交接文档待办：v3 用户验收、积分系统代码审计、省 token 方案、产品 agent 化、Pro 版设计

---

## ⑥ 架构深化机会候选（improve-codebase-architecture 扫描 · 2026-08-13）

> 方法：先探索代码库、测量客观指标，再列"深化机会"。以下候选按优先级排序，每个先列信号，暂不设计接口（按技能流程，待用户选一个再深化为 RFC）。

### 客观指标（证据）
- src 总 ~15,962 行；**19 个文件 > 300 行**（MVP P0 单文件上限）。
- `src/lib/storage.ts`：**59 个导出函数 / 857 行**；**19 个页面/组件直接 `import @/lib/storage`**。
- `InspirationBoard.tsx`：**1050 行**（超上限 3.5×，全仓最大文件）。
- shadcn `ui/` 下 sidebar/menubar/dropdown-menu/context-menu/chart 也超 300 行，但属**锁定的自动生成组件（禁止修改）**，不计入重构范围。

### 候选清单

**候选 1 ｜ InspirationBoard 巨型文件（最高优先级，低风险高回报）**
- Cluster：LifePage/sections/InspirationBoard.tsx（1050 行）
- 耦合原因：看板渲染 + 卡片编辑 + 语音转文字 + 视频链接导入 + 录音器 + 月度串联全挤在一个文件
- 依赖类别：模块内聚过低（shallow module）
- 测试影响：当前几乎不可单测；拆分后各子组件可独立测
- 深化方向：拆成 看板列组件 / 卡片编辑器 / 录音器 / 视频链接导入器 + `useInspirations` hook 承载状态逻辑

**候选 2 ｜ UI↔数据层直连（耦合面过大）**
- Cluster：19 个 `src/pages|components` ↔ `src/lib/storage.ts`
- 耦合原因：页面直接调用 `getX/setX` 等 59 个细粒度数据函数，UI 知晓存储实现细节
- 依赖类别：调用模式耦合（call-pattern）
- 测试影响：页面测试被迫依赖 localStorage 真实实现；无法在边界测
- 深化方向：引入领域 service/hook 层（如 `useDailyRecords` / `useLearningProgress`），storage.ts 退化为"深模块"（小接口藏大实现），UI 只依赖稳定领域 API

**候选 3 ｜ storage.ts 接口过宽（59 导出 / 857 行）**
- Cluster：src/lib/storage.ts
- 耦合原因：扁平数据访问层，所有实体 CRUD 平铺在一个文件
- 依赖类别：宽接口 / 低深度
- 测试影响：单测需 mock 整个模块；按域拆分后可分而治之
- 深化方向：按域拆为 `dailyRepo` / `learningRepo` / `lifeRepo` / `statsRepo` 等，提升深度与可导航性（注意：第②决策卡的解耦改造也以此为枢纽，两者可合并执行）

**候选 4 ｜ 多页面系统性超 300 行（浅模块模式）**
- Cluster：EnglishLearning(678) / ProfilePage(582) / AiPmLearning(544) / StatsPage(527) / TodayPage(519) / WeeklyReportSection(468) / FinanceTracker(436) / BrainTracker(383) / AiDevLearning(353) / VibeLearning(328) / FitnessTracker(303)
- 耦合原因：页面把"UI 渲染 + 状态逻辑 + 数据拼装"全写在一起
- 依赖类别：浅模块（接口≈实现）
- 测试影响：逻辑藏在大组件里，难单测
- 深化方向：统一治理——抽 feature 子组件 + 自定义 hook（`useState/useEffect` 逻辑下沉），与候选 2 的 hook 层同源

**候选 5 ｜ @lark-apaas 跨边界依赖（平台耦合）**
- Cluster：storage.ts(scopedStorage) / InspirationBoard(UniversalLink) / index.tsx(AppContainer) / vite&tsconfig preset
- 耦合原因：业务代码直接依赖飞书平台 SDK，无适配层
- 依赖类别：跨边界依赖（ports & adapters 反模式）
- 测试影响：无法脱离飞书运行时做单测
- 深化方向：应用端口-适配器，把平台依赖隔离到 adapter；**已在②决策卡定为"独立化解耦"，此项与决策卡合并执行**

**候选 6 ｜ shared/ 死配置 + 文档漂移（架构卫生）**
- Cluster：vite.config / tsconfig / build.sh / README / 交接文档
- 耦合原因：`@shared` 别名 + build 拷贝指向不存在的 `shared/` 目录；README/交接文档多处与代码不符
- 依赖类别：配置漂移（非模块深化，但需清理以消隐患）
- 深化方向：要么建 `shared/static` 放真实静态资源并启用，要么从配置与文档移除该别名

---

## ⑦ RFC：InspirationBoard 深化重构（候选1 · 用户选定）

> 状态：推荐设计（待执行）。如需提为 GitHub Issue 可告知（需 gh 授权，未自动创建）。

### 问题空间
- `src/pages/LifePage/sections/InspirationBoard.tsx` = **1050 行**（全仓最大，超 300 行上限 3.5×）。
- 单文件同时承担：五列看板渲染、卡片增删改、语音转文字（Web Speech API + iOS 降级）、视频链接导入、录音器（MediaRecorder + MIME 降级链）、月度串联。
- 直接 `import storage`（get/setInspirations）与 `@lark-apaas` `UniversalLink`。
- UI 渲染、状态逻辑、平台 API 调用全部纠缠 → 不可单测，且耦合飞书。

### 约束（新接口需满足）
- 拆分后每个文件 ≤ 300 行（P0 规则）。
- 状态逻辑下沉到 hook，组件只做渲染（单一职责）。
- 平台依赖（UniversalLink / 语音 / 录音）隔离，便于飞书解耦（候选5）。
- 对外行为不变：五列看板、三种记录方式、月度串联、连续天数逻辑保持。

### 推荐设计（深模块）
- **`useInspirations()` hook**（`src/pages/LifePage/hooks/useInspirations.ts`）：封装 inspirations 读/写/增删改 + 月度串联计算 + 语音/录音状态机；对外暴露稳定 API（如 `addInspiration / updateInspiration / removeInspiration / groupByMonth / recordingState`）。
- **拆 5 个展示组件**（均 ≤ 200 行）：
  - `InspirationBoard.tsx`（容器，只组合 hook + 子组件）
  - `InspirationColumn.tsx`（单列：积压/待办/进行中/审核/完成，`w-64` + `touch-pan-x`）
  - `InspirationCardEditor.tsx`（新增/编辑抽屉：文本/分类/来源）
  - `VoiceRecorder.tsx`（录音 + MIME 降级 + 权限提示）
  - `VideoLinkImporter.tsx`（视频链接 → 待办卡片）
  - `MonthSeriesView.tsx`（月度串联）
- storage 调用经 hook 间接发生 → 与候选2/3 的数据层 hook 层同源，可合并治理。

### 依赖策略
- 平台 API（UniversalLink / MediaRecorder / SpeechRecognition）收敛到 `VoiceRecorder` + `VideoLinkImporter` 内部，或经一个 `platform` adapter；容器与其余组件无平台依赖。

### 测试影响
- 前：逻辑藏在大组件，几乎不可单测。
- 后：`useInspirations` 可在边界测（mock storage 实现）；各展示组件可用 RTL 测渲染与交互。

### 执行步骤
1. 抽 `useInspirations` hook（承上启下，先做）。
2. 拆 5 个子组件。
3. 容器重组 + 路由/导航不动。
4. `lint` + `typecheck` + 真机验证（五列横滑 / 录音 / 视频导入 / 月度串联 / 防刷分）。

### 收益
- 单文件从 1050 → 容器≤200 + 5 子组件各≤200，全部达标。
- 状态逻辑可测、可复用，为后续候选2/3 数据层治理铺路。
- 平台依赖隔离，直接降低飞书解耦（候选5）成本。

---

## 附：本次审计用到的核查手段（可复用于后续）
- 数据层对照：`grep __selfsuper_ storage.ts` ↔ 交接文档 key 清单（结果：14/14 一致）
- 平台耦合溯源：`grep @lark-apaas src` → 5 处命中
- 路由孤儿检测：`grep 页面名 src`（AchievementPage 仅自引用）
- 目录真实性：`test -d shared` + `ls *.config.*` 比对文档声明
