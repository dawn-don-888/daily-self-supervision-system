# 跨AI交接文档 · 每日自我监督系统

> 任何AI接手本项目，先读此文件。纯Markdown，跨AI可读。 最后更新：2026-08-12（全量代码落盘完成）

***

## 一、项目是什么

"每日自我监督系统"是东东（24岁，电商美工转AI产品经理）的个人成长工作台Web应用。 集灵感管理、待办看板、学习打卡、记账、健身、健脑、周报于一体。 同时是东东AI PM转行的作品集项目。

* **线上地址**：https://4ktk9f5gbuxq7.feishuapp.com/app/app_17bwfebgv2m

* **飞书需求文档**：https://my.feishu.cn/docx/TfGAdAHXYoiJTJx9uvBcdYMsnVg

* **app_id**：app_17bwfebgv2m（用app_builder_agent编辑时传此ID）

* **源码目录**：D:\工作台\每日自我监督系统\（全量124个文件，已落盘）

## 二、用户是谁

* 东东，24岁，沈阳，零基础不会代码

* 从电商美工转行AI领域，目标AI产品经理

* 自媒体账号"东东"，认知提升赛道

* 偏好：结构化数据驱动、降维通俗解释（菜市场大妈能懂）、简洁界面

* 工作铁律见 D:\生财有术\AI工作手册\AI工作手册.md 第3节（单一事实源；数字我AGENTS.md 已并入手册）

* 视觉偏好：暖橙色（hsl(38 92% 50%)），日记风，移动端优先

## 三、技术栈

* Vite 8 + React 19 + TypeScript 5.9

* Tailwind CSS 4.0 + shadcn UI (new-york style, 72 个组件)

* ECharts / Recharts 数据可视化

* framer-motion 动画

* react-router-dom 7 路由

* localStorage (@lark-apaas/client-toolkit-lite scopedStorage) 数据持久化

* 纯前端应用，无需后端

* 路径别名：@ → src，@shared → shared（⚠️ `shared/` 目录实际不存在，属死配置；见 `架构决策.md` R-3，解耦时一并处理）

## 四、当前版本状态

### v1（已完成）

五大模块：今日打卡、120天AI学习路径、数据统计、成就徽章、我的

### v2（已完成）

修复开始使用按钮bug；新增英语朗读60句、AI产品经理60天+雷达图、Vibe Coding30天、记账、健身、待办；导航重构为今日/学习/生活/数据/我的

### v3（已完成）

* Bug修复：积分刷分漏洞（AWARDED_TODOS防刷）、完成度3/9、页面卡顿、记账按钮遮挡、删除待办扣分、每日一问替换为"今天的卡点是什么"、健身其他自定义

* 新功能：灵感碎片系统（语音转文字/视频链接/五列看板/月度串联）、学习任务自动待办、健脑打卡、每周报告、英语TTS+录音对比、AI PM核心思维卡片、底部导航滚动隐藏、Pro付费接口预留

### 移动端8个Bug（已修复 2026-08-11晚）

1. "开始使用"按钮无反应 → state驱动+全局事件

2. 英语朗读移动端消失 → Tab改grid 4列

3. 学习按钮太大 → segmented pill样式36px高

4. 灵感看板不能横滑 → touch-pan-x + w-64

5. 录音/保存按钮被导航栏遮挡 → z-index层级修正

6. 切换区域白屏 → 移除AnimatePresence，CSS fade-in

7. 录音不兼容 → MIME类型降级+权限提示+手动输入降级

8. TTS不兼容 → 预热+voiceschanged+同步调用栈+lang设置

## 五、关键设计决策

1. **元能力"拆仿练创"是系统灵魂**：拆（建立领域地图）→仿（顶级样本）→练（撞卡点）→创（知识网络化）

2. **灵感看板五列**：积压碎片→待办→进行中→审核→完成

3. **不闭门造车**：每个功能先查GitHub高星项目参考

4. **免费+隐藏Pro**：免费版全功能，Pro入口低调预留（¥18/月占位）

5. **数据本地存储**：不上传服务器，隐私安全

6. **语音用浏览器原生API**：不依赖第三方服务，iOS降级处理

7. **防刷分**：AWARDED_TODOS Set，只在未完成→完成时+2分，取消/删除扣回

8. **OnboardingGuard**：useState读settings，完成后window.__onboardingResolve()回调

## 六、完整文件结构（124个文件）

```
每日自我监督系统/
├── 根目录配置（16个）
│   ├── package.json          # 依赖和脚本
│   ├── index.html            # HTML入口
│   ├── vite.config.ts        # Vite配置
│   ├── tsconfig.json/.app/.node
│   ├── eslint.config.mjs
│   ├── （Tailwind v4 用 CSS `@import`，无 tailwind.config.js / postcss.config.js）
│   ├── components.json       # shadcn配置
│   ├── .gitignore / .npmrc
│   ├── .spark_project
│   ├── README.md / AGENTS.md / LICENSE / VERSION
│   └── 文档：需求整理.md / 开发SOP.md / 交接文档.md / 对话记录.md / 小红书MVP推广文档.md
├── .githooks/pre-commit      # Git钩子
├── public/
│   ├── favicon.svg           # 飞书紫色闪电logo
│   └── icons.svg             # 6个社交媒体图标sprite
├── scripts/
│   ├── build.sh              # 构建脚本
│   ├── dev.mjs               # 开发服务器（端口8001）
│   └── setup-git-hooks.mjs   # Git钩子注册
└── src/
    ├── app.tsx               # 根组件+路由
    ├── index.tsx             # 入口
    ├── index.css             # 全局样式（6行import）
    ├── tailwind-theme.css    # Tailwind主题变量
    ├── typography.css        # 排版样式
    ├── lib/
    │   ├── storage.ts        # 核心数据层（所有localStorage操作+积分+周报）
    │   ├── constants.ts      # 常量（积分/问题/标签/徽章/分类/等级）
    │   ├── utils.ts          # cn()等工具函数
    │   └── chart-colors.ts   # 图表颜色配置
    ├── hooks/
    │   └── use-mobile.ts     # 移动端检测hook
    ├── data/（8个）
    │   ├── today-page.ts     # 今日页静态数据
    │   ├── learning-page.ts  # AI开发120天任务完整数据
    │   ├── aipm-learning.ts  # AI PM 60天任务完整数据
    │   ├── vibe-learning.ts  # Vibe Coding 30天任务完整数据
    │   ├── english-sentences.ts  # 英语60句+方法论
    │   ├── achievement-page.ts   # 成就页数据
    │   ├── stats-page.ts     # 统计页数据
    │   └── profile-page.ts   # 我的页数据
    ├── components/
    │   ├── Layout.tsx        # 布局（OnboardingGuard+Outlet）
    │   ├── BottomTabBar.tsx  # 底部导航（滚动隐藏）
    │   └── ui/（72 个 shadcn 组件，列表以 components.json 为准）
    │       button, card, input, textarea, dialog, alert-dialog,
    │       switch, label, badge, tabs, progress, separator, select,
    │       sheet, drawer, scroll-area, checkbox, radio-group, popover,
    │       tooltip, sonner, spinner, skeleton, empty, alert, accordion,
    │       collapsible, avatar, dropdown-menu, slider, table, toggle,
    │       toggle-group, kbd, item, breadcrumb, navigation-menu,
    │       pagination, menubar, button-group, aspect-ratio, context-menu,
    │       hover-card, image, native-select, calendar, carousel, chart,
    │       command, field, form, input-group, input-otp, resizable, sidebar
    └── pages/（22个）
        ├── TodayPage/（6个）
        │   ├── TodayPage.tsx, MoodCard.tsx, DailyQuestionCard.tsx,
        │   ├── ReviewCard.tsx, InspirationCard.tsx, CompletionCard.tsx
        ├── LearningPage/（5个）
        │   ├── LearningPage.tsx
        │   └── sections/AiDevLearning.tsx, AiPmLearning.tsx,
        │       EnglishLearning.tsx, VibeLearning.tsx
        ├── LifePage/（6个）
        │   ├── LifePage.tsx
        │   └── sections/BrainTracker.tsx, FinanceTracker.tsx,
        │       FitnessTracker.tsx, InspirationBoard.tsx, TodoTracker.tsx
        ├── StatsPage/（2个）
        │   ├── StatsPage.tsx
        │   └── sections/WeeklyReportSection.tsx
        ├── ProfilePage/ProfilePage.tsx
        ├── WelcomeGuide/WelcomeGuide.tsx
        ├── AchievementPage/AchievementPage.tsx
        ├── ExamplePage/ExamplePage.tsx（全注释模板）
        └── NotFoundPage/NotFoundPage.tsx
```

## 七、数据结构速查（storage.ts）

| localStorage Key              | 内容                                                  |
| ----------------------------- | --------------------------------------------------- |
| __selfsuper_daily_records     | 每日打卡（心情/一问/复盘/灵感/学习完成）                              |
| __selfsuper_learning_progress | AI开发120天进度                                          |
| __selfsuper_aipm_progress     | AI PM 60天+四支柱pillarProgress                         |
| __selfsuper_vibe_progress     | Vibe 30天进度                                          |
| __selfsuper_english_progress  | 英语进度（currentDay/streakDays等）                        |
| __selfsuper_achievements      | totalPoints/level/badges/streakDays                 |
| __selfsuper_settings          | onboardingDone/提醒设置                                 |
| __selfsuper_accounting        | 记账records（type/amount/category/date）                |
| __selfsuper_fitness           | 健身records（type/duration/date, streakDays）           |
| __selfsuper_todos             | 待办items（id/text/done/date/completedAt）              |
| __selfsuper_brain             | 健脑records（type/duration, streakDays）                |
| __selfsuper_inspirations      | 灵感cards（id/title/content/sourceType/status/tags...） |
| __selfsuper_weekly_reports    | IWeeklyReport数组                                     |
| __selfsuper_awarded_todos     | 已奖励待办ID数组（防刷分）                                      |

积分：打卡10/复盘15/心情5/灵感10/学习20/连续1/英语15/健身10/记账5/待办2/AIPM20/Vibe20 等级：1新手(0-100) 2学徒(100-300) 3践行者(300-600) 4成长者(600-1000) 5大师(1000+) 徽章：19个

## 八、如何继续开发

1. 读 D:\生财有术\AI工作手册\AI工作手册.md 第3节 了解工作铁律（单一事实源）

2. 读本文件和 对话记录.md 了解完整历史

3. 用app_builder_agent传app_id=app_17bwfebgv2m进行在线编辑

4. 每次改动后检查：积分漏洞、完成度计算、移动端适配

5. 代码改完后同步落盘到D:\工作台\每日自我监督系统\

6. 时间线写入本交接文档「十一、时间线记录」节（用户 2026-08-13 拍板：此项目时间线只留工作台交接，不挂数字我时间线档案）

7. 功能变化同步更新本文档和需求整理.md

## 九、移动端兼容要点（踩坑记录）

* **TTS**：必须在用户点击事件同步调用栈内speechSynthesis.speak()；iOS需要先warmup（getVoices + voiceschanged监听）；必须设utter.lang='en-US'

* **录音**：MediaRecorder MIME类型降级链 webm;codecs=opus → audio/mp4(iOS) → webm → ogg；权限拒绝要提示

* **语音识别**：window.SpeechRecognition || window.webkitSpeechRecognition；iOS Safari不支持，必须降级手动textarea

* **Tab栏**：移动端用grid-cols-4等宽，不要用flex gap导致按钮溢出

* **看板横滑**：容器加overflow-x-auto touch-pan-x，列加w-64 shrink-0

* **弹窗层级**：遮罩z-[60]，抽屉z-[70]，必须高于底部导航z-50

* **页面切换**：不要用AnimatePresence卸载重建（白屏卡顿），用CSS fade-in保持挂载

* **安全区**：底部按钮加pb-[env(safe-area-inset-bottom)]

## 十、待办事项

* [x] v3功能开发完成

* [x] 移动端8个Bug修复

* [x] 全量代码落盘（124个文件）

* [x] 小红书推广文档

* [x] 对话记录和交接文档

* [x] **代码架构审计（2026-08-13，见 `架构决策.md`）** ← 本次新增完成

* [ ] v3功能用户验收

* [ ] **飞书 @lark-apaas 解耦为独立 Web App（Phase0 既定方向，详见 `架构决策.md` 决策卡 + ⑥候选5）** ← 本次新增待办

* [x] **文档漂移修正（交接文档部分，2026-08-13）**：55→72组件 / 删 tailwind.config.js-postcss.config.js / @shared死配置标注 / 铁律引用改指手册 / 时间线改留本地 ✓
* [ ] **README.md 漂移修正**（shared 路径/55vs72/路由名/别名）← 交由前端工程师第④关执行（见卡3）

* [ ] 省token开源方案选型安装

* [ ] 产品agent化方案

* [ ] 灵感月度串联的AI增强

* [ ] Pro版功能设计

## 十（补）、架构决策摘要（2026-08-13 审计产出）

> 完整版见仓库根 `架构决策.md`。此处记关键结论，供快速接手。

* **数据层完整**：`storage.ts`(857行) 实现全部 14 个 localStorage key，与本文档一一对应 ✓

* **平台方向已定（Phase0）**：纯前端 Web App + 本地存储 → 当前代码仍耦合飞书 `@lark-apaas`（scopedStorage/AppContainer/UniversalLink），属方向漂移，需解耦为原生 localStorage（枢纽在 storage.ts 单点 + 4 处轻引用）

* **架构深化 6 候选**：①InspirationBoard 1050行巨型文件 ②UI直连storage(19文件) ③storage 59导出过宽 ④多页面超300行 ⑤飞书跨边界依赖 ⑥shared死配置+文档漂移

* **孤儿代码**：`AchievementPage` 未被路由/导航引用（成就由 ProfilePage 渲染）

* **度量**：src ~15962 行；19 文件 > 300 行（ui/ 锁定的 shadcn 组件除外）

## 十一、时间线记录（2026-08-13 · 架构审计）

> ⚠️ 留痕约定（用户 2026-08-13 指示）：时间线与记忆日志统一记于本工作台交接文档，不外挂其他系统（如数字我时间线档案）。

### 09:27 组建团队 · Phase 0 需求澄清

* 目标：个人自用 + 转行 AI PM 作品集（暂不运营）

* 范围：四段闭环最小可用（灵感→待办→执行→复盘，增删改查 + 本地存）

* 技术形态：**纯前端 Web App（Vite+React，浏览器跑，本地存储）**

* 数据：听架构师建议，倾向本地优先

### 09:49 第①关 架构师 · 代码完整性审计

* 读 `01-架构师.md`，审计 `D:\工作台\每日自我监督系统`（v3.0.0，124 文件）

* 交付 `架构决策.md`：①代码审计表 ②平台决策+决策卡 ③模块边界图 ④技术栈 ⑤风险待办 ⑥架构深化6候选 ⑦InspirationBoard RFC

* 核心发现：数据层完整(14 key)；代码仍耦合飞书@lark-apaas，与 Phase0「纯前端本地存储」背离(方向漂移)

* 调用 decision-master 固化决策卡；improve-codebase-architecture 扫描得 6 候选

* 用户拍板：按推荐深化**候选1 InspirationBoard** 为 RFC；时间线/记忆只留工作台交接

### 待办（下一步）

* 用户从 6 候选选 1 个深化为 RFC（已选候选1，见架构决策.md ⑦）

* 执行飞书解耦（决策卡方案乙）

* 修正文档漂移（README/交接文档与代码不符处）

* 进入第②关 AI PM(PRD/Spec)

## 十一、用户高频词记录

* 灵感碎片、看板、待办、拆仿练创、元能力

* 闭环、复利、ship ugly first、先跑通再优化

* 不闭门造车、GitHub高星项目

* 跨AI、时间线、留痕、交接包

* 免费智能体、产品agent化

* 作品集、转行AI PM

* 语音转文字、录音对比、TTS

* 每周报告、数据可视化

* 付费接口、Pro版

* 今天的卡点是什么、最小一步

***

## 十二、改动交接卡（给前端工程师 · 第④关执行）

> ⚠️ **铁律：本关（大主管/架构师/AI PM）不写代码。** 以下三张卡是"精确改动说明书"，由前端工程师在**独立会话**执行；执行完把 diff / 截图回交本线程，由大主管验收。 三张卡均基于 `架构决策.md` 的②决策卡（方案乙·独立化）与⑥⑦候选。执行顺序建议：卡1（解耦）→ 卡3（文档）→ 卡2（拆分）；卡2 可与卡1 协同（平台依赖一并隔离）。

### 卡1 · 飞书 @lark-apaas 解耦为独立 Web App（方案乙）

**目标**：使项目脱离飞书运行时，成为 `npm run dev` 即可本地跑、可部署任意静态托管的纯前端 Web App（Phase0 既定方向）。

**改动清单（精确文件 + 位置 + 改法）**

| &#x23; | 文件                                                 | 位置                                                                    | 改法                                                                                                                     | 验证                 |
| ------ | -------------------------------------------------- | --------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- | ------------------ |
| 1      | `src/lib/storage.ts`                               | 第1行 `import { scopedStorage } from '@lark-apaas/client-toolkit-lite'` | 改用原生 `localStorage` 封装；保持 `getItem/setItem/removeItem` 同名签名，使 59 个导出函数与全部调用方 **零改动**                                   | 调用方无编译错误           |
| 2      | `src/index.tsx`                                    | 第5行 `import { AppContainer, ErrorRender }`                            | 删除；用 `react-error-boundary`（已在依赖）的 `ErrorBoundary` 直接包 App                                                             | 启动无白屏              |
| 3      | `src/index.css`                                    | 第3行 `@import "@lark-apaas/client-toolkit-lite/styles.css"`            | 整行删除                                                                                                                   | 样式正常               |
| 4      | `src/pages/LifePage/sections/InspirationBoard.tsx` | 第28行 `import { UniversalLink }`；第426-436行渲染 sourceUrl                 | `UniversalLink` → `<a>` 或 react-router `<Link>`                                                                        | 链接可点               |
| 5      | `src/pages/LifePage/sections/BrainTracker.tsx`     | 第109-112行动态 `import('@lark-apaas/client-toolkit-lite')`               | 改用原生 `localStorage.setItem('__selfsuper_brain', JSON.stringify(state))`                                                | 健脑数据落盘             |
| 6      | `vite.config.ts`                                   | `defineConfig` 来自 `@lark-apaas/coding-preset-vite-react`              | 改用 `@vitejs/plugin-react` 标准配置；保留 `@→src` 别名；`@shared` 别名**要么建 `shared/` 放静态资源，要么删除**                                  | `npm run build` 通过 |
| 7      | `tsconfig.app.json`                                | `extends "@lark-apaas/coding-presets-react/..."`                      | 改用标准 react-ts tsconfig（`tsconfig.app.json` 自包含）                                                                        | `typecheck` 通过     |
| 8      | `package.json`                                     | 3 个 `@lark-apaas/*` 依赖                                                | 移除 `client-toolkit-lite` / `coding-presets-react`(dev) / `coding-preset-vite-react`(dev)；确保 `@vitejs/plugin-react` 在依赖 | `npm install` 干净   |

**整体验证**：`npm install` → `npm run typecheck` → `npm run build` → `npm run dev` 真机验证 5 页 + 积分/防刷逻辑。 **⚠️ 风险**：`scopedStorage` 与 `localStorage` 命名空间不同（飞书有前缀），迁移后旧数据在用户浏览器里是飞书命名空间 → 纯个人项目可接受"首次使用重新初始化"，若需保留可在 hook 层做一次一次性迁移读取。 **接手人**：前端工程师（第④关）单独会话执行。

### 卡2 · InspirationBoard.tsx 1050行拆分（RFC ⑦）

**目标**：单文件 ≤300 行（P0 规则），状态逻辑下沉 hook，平台依赖隔离；对外行为不变。 **拆分方案**

* 新建 `src/pages/LifePage/hooks/useInspirations.ts`：封装 inspirations 读/写/增删改 + 月度串联计算 + 语音/录音状态机；对外暴露 `addInspiration / updateInspiration / removeInspiration / groupByMonth / recordingState`。

* 新建 5 个展示组件（各 ≤200 行）：`InspirationColumn.tsx`（五列·`w-64 touch-pan-x`）、`InspirationCardEditor.tsx`（新增/编辑抽屉）、`VoiceRecorder.tsx`（录音+MIME降级+权限提示）、`VideoLinkImporter.tsx`（视频链接→待办）、`MonthSeriesView.tsx`（月度串联）。

* 改写 `InspirationBoard.tsx` 为容器（只组合 hook + 子组件，≤200 行）。 **约束**：五列看板、三种记录方式、月度串联、连续天数逻辑保持；platform API（UniversalLink/MediaRecorder/SpeechRecognition）收敛到 `VoiceRecorder`/`VideoLinkImporter` 内部，与卡1 协同去飞书。 **验证**：`lint` + `typecheck` + 真机（横滑/录音/视频导入/月度串联/防刷分）。 **接手人**：前端工程师（第④关）。

### 卡3 · 文档漂移修正（C-1~C-4）

* **README.md**：静态数据位置改为 `src/data/*.ts`（删 `shared/static/data` 说法）；UI 组件数 `55→72`；首页路由 `<Welcome/>` → `<WelcomeGuide/>`；删 `shared/` 别名相关描述或注明未启用。

* **交接文档.md**：✓ 已修（2026-08-13，AI PM/大主管执行）：根目录清单删 `tailwind.config.js / postcss.config.js`（实际不存在，v4 用 CSS `@import`）；技术栈 `55→72` 组件；@shared死配置标注；铁律引用改指手册；时间线改留本地。仅剩：localStorage 说明待改为"解耦后为原生 localStorage"（卡1 完成后由前端工程师改，见下方 line 45 当前仍耦合）。

* **接手人**：前端/文档 owner（可与卡1/2 同会话）。

